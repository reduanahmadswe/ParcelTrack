NODE_ENV=production
PORT=3000
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/parcel-delivery?retryWrites=true&w=majority
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d
BCRYPT_SALT_ROUNDS=12

# For Vercel deployment:
# 1. Create a MongoDB Atlas cluster (free tier available)
# 2. Replace the MONGODB_URI with your Atlas connection string
# 3. Generate a strong JWT_SECRET using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# 4. Add these variables to your Vercel project settings
